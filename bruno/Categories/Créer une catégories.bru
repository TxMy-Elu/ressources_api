meta {
  name: Créer une catégories
  type: http
  seq: 2
}

post {
  url: https://localhost:8000/api/categories
  body: json
  auth: bearer
}

body:json {
  {
      "nom": "Ecole",
      "description": "Liste des école dans le domaine de la santé"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
